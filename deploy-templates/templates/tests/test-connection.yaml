apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "test-dn-edp.fullname" . }}-test-connection"
  labels:
    {{- include "test-dn-edp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "test-dn-edp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
