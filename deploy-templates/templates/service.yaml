apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-dn-edp.fullname" . }}
  labels:
    {{- include "test-dn-edp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-dn-edp.selectorLabels" . | nindent 4 }}
